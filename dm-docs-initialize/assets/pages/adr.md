# ADR-NNN: One-line statement of the decision

## Status

Proposed (YYYY-MM-DD, Name)

## Context

<!-- Why a decision is needed now. What goes wrong without one. Two or three
paragraphs at most. No Jira keys, no inventory of current files, no names that change
per iteration; an ADR is read for years. -->

## Decision

<!-- The constraint, in one or two sentences, in bold. Then the one verifiable
consequence that makes it enforceable: a test, a type, a lint rule, a schema
constraint, a config default. -->

## Consequences

<!-- What becomes easier, what becomes harder, what it costs. Where the running system
knowingly departs from this decision, point to the EX-nnn entry in
../exceptions.md so a reader is not misled. -->

<!-- Add "## Alternatives considered" only when someone disputes the decision, and
write only the section that answers the dispute. -->
